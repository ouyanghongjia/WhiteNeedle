# Dependencies
node_modules/

# Build outputs
ios-dylib/build/
**/DerivedData/
**/build/
**/dist/
**/out/
*.o

# Xcode / CocoaPods
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.ipa
*.dSYM.zip
*.dSYM

# Logs & local artifacts
log.txt
*.log
.DS_Store

# Release artifacts (use GitHub Releases instead; vsix is large)
*.vsix

# Pre-built dylib binaries — only dist/ (via build-dist.sh) should contain them
ios-dylib/WhiteNeedle.dylib
skills/whiteneedle-resign/payload/WhiteNeedle.dylib

# Secrets & signing (never commit)
.env
.env.*
*.mobileprovision
*.p12
*.pem
AuthKey_*.p8
deploy.conf.local

# Editor / tooling
.ruby-lsp/
.idea/

# Optional: local Cursor rules with machine paths (uncomment if needed)
# .cursor/
