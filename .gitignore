# Dependencies
node_modules/

# Build outputs
ios-dylib/build/
**/DerivedData/
**/build/
**/dist/
**/out/
*.o

# Xcode / CocoaPods
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.ipa
*.dSYM.zip
*.dSYM

# Logs & local artifacts
log.txt
*.log
.DS_Store

# Release artifacts (use GitHub Releases instead; vsix is large)
*.vsix

# Pre-built binaries — build from source, don't commit
skills/whiteneedle-resign/payload/WhiteNeedle.framework/

# Secrets & signing (never commit)
.env
.env.*
*.mobileprovision
*.p12
*.pem
AuthKey_*.p8
deploy.conf.local

# WebDriverAgent (auto-setup via tools/setup-wda.sh)
.wda/

# Editor / tooling
.ruby-lsp/
.idea/
jsconfig.json

# Optional: local Cursor rules with machine paths (uncomment if needed)
# .cursor/
