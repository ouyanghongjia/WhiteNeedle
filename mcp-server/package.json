{
  "name": "whiteneedle-mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing WhiteNeedle iOS runtime capabilities to AI IDEs",
  "main": "dist/index.js",
  "bin": {
    "whiteneedle-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25 || ^4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.2"
  }
}
